#ifndef PLAY_MODE_H
#define PLAY_MODE_H

// ═══════════════════════════════════════════════════════════════════════════
//  ЛОГИКА РЕЖИМА ИГРЫ
// ═══════════════════════════════════════════════════════════════════════════

/**
 * Обработать режим игры:
 * - встроенная клавиатура
 * - матричная клавиатура
 * - выход в меню (Backspace)
 * 
 * @param now - текущее время в миллисекундах
 * @param matrixChanged - была ли изменена матричная клавиатура
 * @return true если экран нужно перерисовать
 */
bool handlePlayMode(unsigned long now, bool matrixChanged);

#endif // PLAY_MODE_H
